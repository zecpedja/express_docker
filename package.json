{
  "name": "express-docker-app",
  "version": "1.0.0",
  "description": "A Node.js and React app with Express backend, ready for Docker and AWS App Runner.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "author": "PZ",
  "license": "MIT"
}
